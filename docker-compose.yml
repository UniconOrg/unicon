services:
  unicon:
    build: 
      context: .
      dockerfile: docker_images/app/Dockerfile.app
    ports:
      - "3001:3000"
    environment:
      BASE_PATH: ""
      NEXT_PUBLIC_BASE_PATH: ""
